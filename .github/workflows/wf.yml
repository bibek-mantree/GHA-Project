name: npm ci

on:
    push:
        branches:
            - main
            - feat-*


jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
              name: Checkout
